{
  "name": "dose",
  "version": "1.0.0",
  "description": "Daily med & mood check-in",
  "private": true,
  "scripts": {
    "www": "rm -rf www && mkdir -p www && cp index.html manifest.json sw.js icon-192.png icon-512.png apple-touch-icon.png www/",
    "ios": "npm run www && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1"
  }
}
